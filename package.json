{
  "name": "signalmode",
  "version": "2.1.1",
  "description": "SignalMode — Signal over noise. LLM-agnostic communication optimizer for AI coding agents.",
  "license": "MIT",
  "author": "wfprieto",
  "homepage": "https://github.com/wfprieto/SignalMode",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wfprieto/SignalMode.git"
  },
  "bugs": {
    "url": "https://github.com/wfprieto/SignalMode/issues"
  },
  "bin": {
    "signalmode": "./bin/install.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "generate": "node scripts/generate-platforms.mjs",
    "validate": "node scripts/validate-repository.mjs",
    "lint": "node scripts/lint-prompts.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "test:installer": "node --test tests/installer/*.test.mjs",
    "test:policies": "node --test tests/policies/*.test.mjs",
    "check": "npm run validate && npm run lint",
    "precommit": "npm run check",
    "prepublishOnly": "npm run ci",
    "ci": "npm run generate:check && npm run validate && npm run lint && npm test && npm run smoke",
    "smoke": "node bin/install.js --help && node bin/install.js --list",
    "generate:check": "node scripts/generate-platforms.mjs && git diff --exit-code -- platforms/ || (echo \"FAIL: platform files are stale, run npm run generate\" && exit 1)"
  },
  "files": [
    "bin/",
    "src/",
    "agents/",
    "skills/",
    "platforms/",
    "rules/",
    "scripts/",
    "README.md",
    "LICENSE",
    "INSTALL.md"
  ]
}
